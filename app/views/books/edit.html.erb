<h1>Editing Book</h1>
<%= form_for(@bookf) do |f| %>

    <h4>title</h4>
    <%= f.text_field :title %>

    <h4>opinion</h4>
    <%= f.text_area :body %>
<%= f.submit 'Update Book' %>
<% end %>
<%= link_to "Show", book_path(@bookf) %>
<%= link_to "Back", books_path %>